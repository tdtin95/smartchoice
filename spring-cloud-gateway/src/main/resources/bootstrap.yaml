spring:
  application:
    name: spring-cloud-gateway
  cloud:
    config:
      profile: default
      uri: http://localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: "*"