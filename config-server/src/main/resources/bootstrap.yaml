spring:
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  application:
    name: config-server
management:
  endpoints:
    web:
      exposure:
        include: "*"