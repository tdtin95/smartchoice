spring:
  application:
    name: product-adapter-service
  cloud:
    config:
      profile: production
      uri: http://config-server:8080
management:
  endpoints:
    web:
      exposure:
        include: "*"